/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Future: Configure webpack for Monaco Editor
  // webpack: (config) => { ... }
};

export default nextConfig;
