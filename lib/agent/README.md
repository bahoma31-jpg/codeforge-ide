# 🤖 CodeForge IDE — Standalone AI Agent

## المعمارية

```
lib/agent/
├── types.ts                 # جميع الأنواع (19 interface/type)
├── constants.ts             # الثوابت
├── agent-service.ts         # المحرك الرئيسي (Tool Calling Loop)
├── providers/
│   ├── index.ts             # مصنع المزودين + التهيئة
│   ├── openai.ts            # OpenAI (GPT-4o, GPT-4.1)
│   ├── google.ts            # Google Gemini (2.0 Flash, 1.5 Pro)
│   ├── groq.ts              # Groq (Llama 3.3, Mixtral)
│   └── anthropic.ts         # Anthropic (Claude Sonnet 4, Haiku 3.5)
├── tools/
│   ├── index.ts             # سجل الأدوات
│   ├── file-tools.ts        # 9 أدوات نظام ملفات
│   ├── git-tools.ts         # 7 أدوات Git
│   └── utility-tools.ts     # 3 أدوات مساعدة
├── safety/
│   ├── risk-classifier.ts   # مصنف الخطورة
│   └── approval-manager.ts  # مدير التأكيدات
└── hooks/
    ├── index.ts             # التصديرات
    ├── use-agent-context-menu.ts  # قائمة سياق المحرر
    └── use-agent-keyboard.ts     # اختصارات لوحة المفاتيح

components/agent/
├── index.ts                 # التصديرات
├── agent-panel.tsx          # اللوحة الرئيسية (محادثة + تأكيدات + سجل)
├── chat-message.tsx         # فقاعات الرسائل + Markdown
├── chat-input.tsx           # حقل الإدخال + إجراءات سريعة
├── tool-call-status.tsx     # مؤشر تنفيذ الأدوات
├── approval-dialog.tsx      # نافذة التأكيد
├── agent-settings.tsx       # إعدادات الوكيل
└── agent-toggle-button.tsx  # زر الفتح/الإغلاق

lib/stores/
└── agent-store.ts           # Zustand state management
```

## المزودون

| المزود        | النماذج                    | التكلفة     | الميزة          |
| ------------- | -------------------------- | ----------- | --------------- |
| OpenAI        | GPT-4o, GPT-4.1, Mini      | مدفوع       | الأقوى في Tools |
| Google Gemini | 2.0 Flash/Lite, 1.5 Pro    | مجاني/مدفوع | سياق طويل جداً  |
| Groq          | Llama 3.3, Mixtral         | مجاني       | الأسرع          |
| Anthropic     | Claude Sonnet 4, Haiku 3.5 | مدفوع       | الأفضل للكود    |

## الأدوات (19 أداة)

### نظام الملفات (9)

| الأداة          | الخطورة   | الوصف             |
| --------------- | --------- | ----------------- |
| `list_files`    | 🟢 تلقائي | عرض قائمة الملفات |
| `read_file`     | 🟢 تلقائي | قراءة محتوى ملف   |
| `search_files`  | 🟢 تلقائي | البحث في الملفات  |
| `create_file`   | 🟡 بإشعار | إنشاء ملف جديد    |
| `update_file`   | 🟡 بإشعار | تحديث محتوى ملف   |
| `create_folder` | 🟡 بإشعار | إنشاء مجلد        |
| `rename_file`   | 🟡 بإشعار | إعادة تسمية       |
| `move_file`     | 🟡 بإشعار | نقل ملف           |
| `delete_file`   | 🔴 بتأكيد | حذف ملف/مجلد      |

### Git (7)

| الأداة              | الخطورة   | الوصف              |
| ------------------- | --------- | ------------------ |
| `git_status`        | 🟢 تلقائي | حالة المستودع      |
| `git_diff`          | 🟢 تلقائي | عرض التغييرات      |
| `git_stage`         | 🟡 بإشعار | تجهيز ملفات        |
| `git_commit`        | 🟡 بإشعار | حفظ التغييرات      |
| `git_create_branch` | 🟡 بإشعار | إنشاء فرع          |
| `git_push`          | 🔴 بتأكيد | الدفع لـ GitHub    |
| `git_create_pr`     | 🔴 بتأكيد | إنشاء Pull Request |

### مساعدة (3)

| الأداة                | الخطورة   | الوصف           |
| --------------------- | --------- | --------------- |
| `get_project_context` | 🟢 تلقائي | معلومات المشروع |
| `explain_code`        | 🟢 تلقائي | شرح الكود       |
| `suggest_fix`         | 🟢 تلقائي | اقتراح إصلاح    |

## مستويات الأمان

```
🟢 auto    → تنفيذ تلقائي بدون تدخل المستخدم (قراءة فقط)
🟡 notify  → تنفيذ مع إشعار المستخدم (كتابة عادية)
🔴 confirm → يتطلب موافقة المستخدم (حذف، دفع، PR)
```

## اختصارات لوحة المفاتيح

| الاختصار       | الوظيفة               |
| -------------- | --------------------- |
| `Ctrl+Shift+A` | فتح/إغلاق لوحة الوكيل |
| `Enter`        | إرسال رسالة           |
| `Shift+Enter`  | سطر جديد              |
| `Escape`       | إغلاق اللوحة          |

## التكامل

```tsx
// في اللياوت الرئيسي
import { AgentPanel, AgentToggleButton } from '@/components/agent';
import { useAgentKeyboard } from '@/lib/agent/hooks';

// في الشريط الجانبي
<AgentToggleButton />

// في اللوحة الجانبية
<AgentPanel />

// في Context Menu المحرر
import { useAgentContextMenu } from '@/lib/agent/hooks';
const { actions } = useAgentContextMenu();
```
